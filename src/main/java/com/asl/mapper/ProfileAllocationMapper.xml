<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asl.mapper.ProfileAllocationMapper">

	<insert id="save" 
			parameterType="ProfileAllocation">
		INSERT INTO ProfileAllocation
		(
			paid,
			username,
			reportProfileId,
			menuProfileId,
			userProfileId,
			businessId,
			createdBy,
			lastModifiedBy,
			createdByIP,
			lastModifiedByIP,
			status,
			createDate,
			updateDate
		)
		VALUES
		(
			PROFILE_ALLOCATION_SEQ.nextval,
			#{username, jdbcType=VARCHAR},
			#{reportProfileId, jdbcType=NUMBER},
			#{menuProfileId, jdbcType=NUMBER},
			#{userProfileId, jdbcType=NUMBER},
			#{businessId, jdbcType=VARCHAR},
			#{createdBy, jdbcType=VARCHAR},
			#{lastModifiedBy, jdbcType=VARCHAR},
			#{createdByIP, jdbcType=VARCHAR},
			#{lastModifiedByIP, jdbcType=VARCHAR},
			#{status, jdbcType=VARCHAR},
			#{createDate, jdbcType=DATE},
			#{updateDate, jdbcType=DATE}
		)
	</insert>

	<update id="update" 
			parameterType="ProfileAllocation">
		UPDATE
			ProfileAllocation
		SET
			username=#{username, jdbcType=VARCHAR},
			reportProfileId=#{reportProfileId, jdbcType=NUMBER},
			menuProfileId=#{menuProfileId, jdbcType=NUMBER},
			userProfileId=#{userProfileId, jdbcType=NUMBER},
			businessId=#{businessId, jdbcType=VARCHAR},
			createdBy=#{createdBy, jdbcType=VARCHAR},
			lastModifiedBy=#{lastModifiedBy, jdbcType=VARCHAR},
			createdByIP=#{createdByIP, jdbcType=VARCHAR},
			lastModifiedByIP=#{lastModifiedByIP, jdbcType=VARCHAR},
			status=#{status, jdbcType=VARCHAR},
			createDate=#{createDate, jdbcType=DATE},
			updateDate=#{updateDate, jdbcType=DATE}
		WHERE
			paid=#{paid}
	</update>

	<select id="findById" resultType="ProfileAllocation">
		SELECT * FROM ProfileAllocation WHERE profileAllocationId=#{profileAllocationId}
	</select>

	<select id="getAllProfileAllocation" resultType="ProfileAllocation">
		SELECT * FROM ProfileAllocation WHERE businessId=#{businessId}
	</select>

	<select id="findByUsername" resultType="ProfileAllocation">
		SELECT * FROM ProfileAllocation WHERE username=#{username} AND businessId=#{businessId}
	</select>

	<select id="findByUsernameAndPassword" resultType="Users">
		SELECT * FROM Users WHERE username=#{username} AND pswd=#{password}
	</select>

</mapper>