<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asl.mapper.PocrnMapper">

	<insert id="savePocrnHeader" parameterType="Pocrnheader">
<!-- 		<selectKey keyProperty="xbomkey" resultType="String" order="BEFORE">
			SELECT Func_GetTrn('${zid}', '${xbomkey}', 6) FROM DUAL
		</selectKey> -->
		INSERT INTO Pocrnheader
		(   
			xcrmnum,
			zemail,
			xgrnnum,
			xdate,
			xvoucher,
			xsup,
			xstatusgrn,
			xwh,
			xref,
			xstatusap,
			xpaymenttype,
			xstatusjv,
			xnote,
			xtype,
			xdategl,
			
			zid,
			zauserid,
			zuuserid,
			zaip,
			zuip,
			zactive,
			ztime,
			zutime
		)
		VALUES
		(
			#{xcrmnum, jdbcType=VARCHAR},
			#{zemail, jdbcType=VARCHAR},
			#{xgrnnum, jdbcType=VARCHAR},
			#{xdate, jdbcType=DATE},
			#{xvoucher, jdbcType=VARCHAR},
			#{xsup, jdbcType=VARCHAR},
			#{xstatusgrn, jdbcType=VARCHAR},
			#{xwh, jdbcType=VARCHAR},
			#{xref, jdbcType=VARCHAR},
			#{xstatusap, jdbcType=VARCHAR},
			#{xpaymenttype, jdbcType=VARCHAR},
			#{xstatusjv, jdbcType=VARCHAR},
			#{xnote, jdbcType=VARCHAR},
			#{xtype, jdbcType=VARCHAR},
			#{xdategl, jdbcType=DATE},
			
			#{zid, jdbcType=VARCHAR},
			#{zauserid, jdbcType=VARCHAR},
			#{zuuserid, jdbcType=VARCHAR},
			#{zaip, jdbcType=VARCHAR},
			#{zuip, jdbcType=VARCHAR},
			#{zactive, jdbcType=VARCHAR},
			#{ztime, jdbcType=TIMESTAMP},
			#{zutime, jdbcType=TIMESTAMP}
		) 
	</insert>

	<insert id="savePocrnDetail" parameterType="Pocrndetail">
		<!-- <selectKey keyProperty="xbomkey" resultType="String" order="BEFORE">
			SELECT COUNT(*) + 1 FROM BmBomDetail WHERE xbomkey=#{xbomkey} AND zactive='1' AND zid=#{zid}
		</selectKey> -->
		INSERT INTO Pocrndetail
		(
			xrow,
			xcrnnum,
			zemail,
			xunit,
			xitem,
			xqtyord,
			xqtygrn,
			xrate,
			xdocrow,
			xlong,

			zid,
			zauserid,
			zuuserid,
			zaip,
			zuip,
			zactive,
			ztime,
			zutime
		)
		VALUES
		(
			#{xrow, jdbcType=INTEGER},
			#{xcrnnum, jdbcType=VARCHAR},
			#{zemail, jdbcType=VARCHAR},
			#{xunit, jdbcType=VARCHAR},
			#{xitem, jdbcType=VARCHAR},
			#{xqtyord, jdbcType=INTEGER},
			#{xqtygrn, jdbcType=INTEGER},
			#{xrate, jdbcType=INTEGER},
			#{xdocrow, jdbcType=INTEGER},
			#{xlong, jdbcType=VARCHAR},
			
			#{zid, jdbcType=VARCHAR},
			#{zauserid, jdbcType=VARCHAR},
			#{zuuserid, jdbcType=VARCHAR},
			#{zaip, jdbcType=VARCHAR},
			#{zuip, jdbcType=VARCHAR},
			#{zactive, jdbcType=VARCHAR},
			#{ztime, jdbcType=TIMESTAMP},
			#{zutime, jdbcType=TIMESTAMP}
		) 
	</insert>

	<update id="updatePocrnDetail" parameterType="Pocrndetail">
		UPDATE 
			Pocrndetail
		SET
			zemail=#{zemail, jdbcType=VARCHAR},
			xunit=#{xunit, jdbcType=VARCHAR},
			xitem=#{xitem, jdbcType=VARCHAR},
			xqtyord=#{xqtyord, jdbcType=INTEGER},
			xqtygrn=#{xqtygrn, jdbcType=INTEGER},
			xrate=#{xrate, jdbcType=INTEGER},
			xdocrow=#{xdocrow, jdbcType=INTEGER},
			xlong=#{xlong, jdbcType=VARCHAR},
			
			zauserid=#{zauserid, jdbcType=VARCHAR},
			zuuserid=#{zuuserid, jdbcType=VARCHAR},
			zaip=#{zaip, jdbcType=VARCHAR},
			zuip=#{zuip, jdbcType=VARCHAR},
			zactive=#{zactive, jdbcType=VARCHAR},
			ztime=#{ztime, jdbcType=TIMESTAMP},
			zutime=#{zutime, jdbcType=TIMESTAMP}
		WHERE
			xrow=#{xrow, jdbcType=INTEGER}
			AND xcrnnum=#{xcrnnum, jdbcType=VARCHAR}
			AND zid=#{zid, jdbcType=VARCHAR}
	</update>

	<select id="findPocrnHeaderByXcrnnum" resultType="Pocrnheader">
		SELECT * FROM Pocrnheader WHERE xbatch=#{xcrnnum} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="findPocrndetailByXrowAndXcrnnum" resultType="Pocrndetail">
		SELECT * FROM Pocrndetail WHERE xrow=#{xrow} AND xbatch=#{xcrnnum} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="findPocrndetailByXrow" resultType="Pocrndetail">
		SELECT * FROM Pocrndetail WHERE xrow=#{xrow} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="getAllMoheader" resultType="Pocrnheader">
		SELECT * FROM getAllPocrnheader WHERE zid=#{zid} AND zactive='1'
	</select>

</mapper>