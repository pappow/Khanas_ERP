<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asl.mapper.OpdoMapper">

	<insert id="saveOpDoHeader" parameterType="Opdoheader">
<!-- 		<selectKey keyProperty="xbomkey" resultType="String" order="BEFORE">
			SELECT Func_GetTrn('${zid}', '${xbomkey}', 6) FROM DUAL
		</selectKey> -->
		INSERT INTO Opdoheader
		(   
			xdornum,
			xemail,
			xdate,
			xcus,
			xtotamt,
			xvatamt,
			xperson,
			xpaymenttype,
			xdiscamt,
			xordernum,
			xstatusord,
			xstatusjv,
			xpaid,
			xref,
			xnote,
			xchange,
			xpaystatus,
			xtype,
			xstatusar,
			xstaff,
			xwh,
			xdatecom,
			xdatedue,
			xcur,
			xexch,
			xvoucher,
			
			zid,
			zauserid,
			zuuserid,
			zaip,
			zuip,
			zactive,
			ztime,
			zutime
		)
		VALUES
		(
			#{xdornum, jdbcType=VARCHAR},
			#{zemail, jdbcType=VARCHAR},
			#{xdate, jdbcType=DATE},
			#{xcus, jdbcType=VARCHAR},
			#{xtotamt, jdbcType=INTEGER},
			#{xvatamt, jdbcType=INTEGER},
			#{xperson, jdbcType=VARCHAR},
			#{xpaymenttype, jdbcType=VARCHAR},
			#{xdiscamt, jdbcType=INTEGER},
			#{xordernum, jdbcType=VARCHAR},
			#{xstatusord, jdbcType=VARCHAR},
			#{xstatusjv, jdbcType=VARCHAR},
			#{xpaid, jdbcType=INTEGER},
			#{xref, jdbcType=VARCHAR},
			#{xnote, jdbcType=VARCHAR},
			#{xchange, jdbcType=INTEGER},
			#{xpaystatus, jdbcType=VARCHAR},
			#{xtype, jdbcType=VARCHAR},
			#{xstatusar, jdbcType=VARCHAR},
			#{xstaff, jdbcType=VARCHAR},
			#{xwh, jdbcType=VARCHAR},
			#{xdatecom, jdbcType=DATE},
			#{xdatedue, jdbcType=DATE},
			#{xcur, jdbcType=VARCHAR},
			#{xexch, jdbcType=INTEGER},
			#{xvoucher, jdbcType=VARCHAR},
			
			#{zid, jdbcType=VARCHAR},
			#{zauserid, jdbcType=VARCHAR},
			#{zuuserid, jdbcType=VARCHAR},
			#{zaip, jdbcType=VARCHAR},
			#{zuip, jdbcType=VARCHAR},
			#{zactive, jdbcType=VARCHAR},
			#{ztime, jdbcType=TIMESTAMP},
			#{zutime, jdbcType=TIMESTAMP}
		) 
	</insert>

	<insert id="saveOpDoDetail" parameterType="Opdodetail">
		<!-- <selectKey keyProperty="xbomkey" resultType="String" order="BEFORE">
			SELECT COUNT(*) + 1 FROM BmBomDetail WHERE xbomkey=#{xbomkey} AND zactive='1' AND zid=#{zid}
		</selectKey> -->
		INSERT INTO Opdodetail
		(
			xrow,
			xdornum,
			zemail,
			xunitsel,
			xitem,
			xqtyord,
			xrate,
			xcomtype,
			xdorrow,
			xlong,
			xrategrn,
			xqtycrn,

			zid,
			zauserid,
			zuuserid,
			zaip,
			zuip,
			zactive,
			ztime,
			zutime
		)
		VALUES
		(
			#{xrow, jdbcType=INTEGER},
			#{xdornum, jdbcType=VARCHAR},
			#{zemail, jdbcType=VARCHAR},
			#{xunitsel, jdbcType=VARCHAR},
			#{xitem, jdbcType=VARCHAR},
			#{xqtyord, jdbcType=INTEGER},
			#{xrate, jdbcType=INTEGER},
			#{xcomtype, jdbcType=VARCHAR},
			#{xdorrow, jdbcType=INTEGER},
			#{xlong, jdbcType=VARCHAR},
			#{xrategrn, jdbcType=INTEGER},
			#{xqtycrn, jdbcType=INTEGER},
			
			#{zid, jdbcType=VARCHAR},
			#{zauserid, jdbcType=VARCHAR},
			#{zuuserid, jdbcType=VARCHAR},
			#{zaip, jdbcType=VARCHAR},
			#{zuip, jdbcType=VARCHAR},
			#{zactive, jdbcType=VARCHAR},
			#{ztime, jdbcType=TIMESTAMP},
			#{zutime, jdbcType=TIMESTAMP}
		) 
	</insert>

	<update id="updateOpDoDetail" parameterType="Opdodetail">
		UPDATE 
			Opdodetail
		SET
			zemail=#{zemail, jdbcType=VARCHAR},
			xunitsel=#{xunitsel, jdbcType=VARCHAR},
			xitem=#{xitem, jdbcType=VARCHAR},
			xqtyord=#{xqtyord, jdbcType=INTEGER},
			xrate=#{xrate, jdbcType=INTEGER},
			xcomtype=#{xcomtype, jdbcType=VARCHAR},
			xdorrow=#{xdorrow, jdbcType=INTEGER},
			xlong=#{xlong, jdbcType=VARCHAR},
			xrategrn=#{xrategrn, jdbcType=INTEGER},
			xqtycrn=#{xqtycrn, jdbcType=INTEGER},
			
			zauserid=#{zauserid, jdbcType=VARCHAR},
			zuuserid=#{zuuserid, jdbcType=VARCHAR},
			zaip=#{zaip, jdbcType=VARCHAR},
			zuip=#{zuip, jdbcType=VARCHAR},
			zactive=#{zactive, jdbcType=VARCHAR},
			ztime=#{ztime, jdbcType=TIMESTAMP},
			zutime=#{zutime, jdbcType=TIMESTAMP}
		WHERE
			xrow=#{xrow, jdbcType=INTEGER}
			AND xdornum=#{xdornum, jdbcType=VARCHAR}
			AND zid=#{zid, jdbcType=VARCHAR}
	</update>

	<select id="findOpDoHeaderByXdornum" resultType="Opdoheader">
		SELECT * FROM Opdoheader WHERE xdornum=#{xdornum} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="findOpDoDetailByXrowAndXdornum" resultType="Opdodetail">
		SELECT * FROM Opdodetail WHERE xrow=#{xrow} AND xdornum=#{xdornum} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="findOpDoDetailByXrow" resultType="Opdodetail">
		SELECT * FROM Opdodetail WHERE xrow=#{xrow} AND zid=#{zid} AND zactive='1'
	</select>

	<select id="getAllOpDoHeader" resultType="Opdoheader">
		SELECT * FROM Opdoheader WHERE zid=#{zid} AND zactive='1'
	</select>
</mapper>