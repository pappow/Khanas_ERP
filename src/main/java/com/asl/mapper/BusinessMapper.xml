<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.asl.mapper.BusinessMapper">

	<select id="findByBusinessId" resultType="ASLBusiness">
		SELECT * FROM ASLBusiness WHERE businessId=#{businessId}
	</select>

</mapper>